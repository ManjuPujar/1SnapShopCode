'use strict';

/**
 * This decorator is extended to include extra properties or
 * to rebase the existing properties
 */

var baseIndex = module.superModule;

baseIndex.recommendations = require('*/cartridge/models/product/decorators/recommendations');

module.exports = baseIndex;
